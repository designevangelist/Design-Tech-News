---
title: "What ref() and reactive() Are Actually For"
category: "Tools"
date: "Jul 7, 2026"
excerpt: "<p>Vue 3 gives you two ways to make something reactive: <code>ref()</code> and <code>reactive()</code>. The docs explain the difference clearly enough. What they don't tell you is which one to reach f"
icon: "🛠️"
link: "https://dev.to/ysndmr/what-ref-and-reactive-are-actually-for-c52"
---

<p>Vue 3 gives you two ways to make something reactive: <code>ref()</code> and <code>reactive()</code>. The docs explain the difference clearly enough. What they don't tell you is which one to reach for by default.</p> <p>The short answer: <strong><code>ref()</code>, almost always.</strong> <code>reactive()</code> for specific situations where you know what you're trading away.</p> <p>Here's the p

## Read More

[Read the full article](https://dev.to/ysndmr/what-ref-and-reactive-are-actually-for-c52)
